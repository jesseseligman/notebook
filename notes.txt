less notes.txt

# can be used to move a file, the first argument is the file, the second is
# where you want to move it. you can also rename the file
mv

# print working directory
pwd

# short for ls -hl
ll

# concatenate
cat

# search for brew inside of notes.txt
grep brew notes.txt


cat notes.txt

# clears screen in terminal
clear

# shows history of commands within terminal
history

# searches for 'd' inside of notes.txt
grep d notes.txt

# appends 'hello frank' to notes.txt
echo 'hello frank' >> notes.txt


-rw-r--r--  1 jesseseligman  staff    26B May  3 14:31 notes.txt
total 8

# applies the command 'pbcopy' to the output of the command 'll'
ll | pbcopy

# searches for 's' in the output of cat notes.txt
cat notes.txt | grep s

# appends 'hello world' to the file notes.txt
echo 'hello world' >> notes.txt

# overwrites notes.txt with 'this is notes'
echo 'this is notes' > notes.txt

# overwrites notes.txt with "hello world"
echo 'hello world' > notes.txt

# returns back "hello world"
echo 'hello world'

# shows the manual page for the command 'rm'
man rm

# removes the file 'whatever.txt'
rm whatever.txt


cat whatever.txt

# concatenates notes.txt and whatever.txt
cat notes.txt whatever.txt

# copies notes.txt as a new file named 'whatever.txt'
cp notes.txt whatever.txt

# opens notes.txt in atom
atom notes.txt

# opens notes.txt in default '.txt' program
open notes.txt

# creates a file called 'notes.txt'
touch notes.txt


cd notebook/
mkdir notebook
cd week1
mkdir week1
cd galvanize/
cd ga
mkdir galvanize
rmdir Galvanize/
cd ..
rmdir week1/
rmdir notebook
..
rmdir week1
rmdir Galvanize/week1
cd Notebook
cd week1/
cd Galvanize/
cd G
mkdir Galvanize
cd Projects/
cd Applications/
cd a
cd ~
ls
cd /Applications/
ls .
cd Library/PubSub/Feeds/
cd Library/Cookies/
cd ../..
cd assets/stylesheets/
cd my\ site/
cd Galvanize\ Prep/
cd Documents
la /
cd
cd /Users/jesseseligman/
echo $PATH
id
ls -hl
cd /
tree -L 10
tree -L 2
tree -L 1
brew install tree
man echo
echo ~
ls -a
ls -h
echo
open Documents
open Documents/
open .
ls -n
ls -r
ls -ltT
ls -lT
ls -tl
ls -t
ls -i
man ls
ls .DS_Store
ls -hal
ls -halS
ls -aS
ls -S
cat .config/fish/config.fish
ll .config/fish
ll .config
ls .config
la
ls Documents
ls Desktop
ls -l
groups
whoami
open http://davidblaine.surge.sh
surge
git commit -m 'Add a CNAME'
git add CNAME
touch CNAME
git commit -m 'Add a tiny web page'
git add index.html
open index.html
atom .
touch index.html
git init
cd davidblaine.surge.sh
mkdir davidblaine.surge.sh
cd ~/Projects
mkdir ~/Projects
surge -V
npm install -g surge
rm ~/Desktop/test.js
node ~/Desktop/test.js
rm node ~/Desktop/test.js
atom ~/Desktop/test.js
node
node -v
which node
brew install node
curl -fsSL https://git.io/vgqFH | sh
git config --global user.email
git config --global user.name
git config --global user.e
git config --global user.name 'Jesse Seligman'
git config --global user.email 'jesseseligman@gmail.com'
which git
git --version
brew install git
which brew
echo $PATH
echo $EDITOR
atom ~/.config/fish/config.fish
brew info fish
fish_update_completions
curl -fsSL https://git.io/vgqFU | ruby
chsh -s /usr/local/bin/fish
echo '/usr/local/bin/fish' | sudo tee -a /etc/shells
brew install fish
brew doctor
brew update
ruby -e "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/master/install)"
ls -G
uname
cd jesseseligman
cd Documents/"Tax Stuff"
cd ../../..
cd Documents/"Galvanize Prep"/"my site"
cd documents/"Galvaniz Prep"/"my site"
cd ../..
cd fine
cd now
cd not
cd what
cd..
cd ok
cd temp
mkdir -p temp/ok/what/not/now/fine
mkdir -p temp/stuff/things/jon/allen/greg
mkdir temp/stuff/things/jon/allen/greg
mkdir temp/stuff/things
mkdir/temp/stuff/things
mkdir temp/stuff
mkdir temp
hostname
big wig
cd ../
cd
cd applications
open "Google Chrome"
open chrome
google-chrome
whereis google-chrome
chrome
apm
bash
.desktop
desktop
nothing.js
atom
run
s
